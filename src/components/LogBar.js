import { AppBar } from "@mui/material";
import { Box } from "@mui/system";

const LogBar = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <AppBar></AppBar>
    </Box>
  );
};
export default LogBar;
